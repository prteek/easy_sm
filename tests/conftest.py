"""
Shared pytest configuration and fixtures.
"""

# This file is intentionally minimal.
# Local command tests now use mocked app directories instead of relying on
# the gitignored app/ directory, so no auto-skip logic is needed.
